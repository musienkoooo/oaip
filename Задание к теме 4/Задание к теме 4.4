#include <iostream>

using namespace std;

int main()
{
	
  int   a2, b2, s, r, p;
  float a, b, f;
  cin >> a >> b;
  a2 = a * a;
  b2 = b * b;
  s = a2 + b2;
  r = abs(a2 - b2);
  p = a2 * b2;
  f = a2 / b2;
  cout << s << " " << r << " " <<  p <<  " " << f;
  
	
	return 0;
}
